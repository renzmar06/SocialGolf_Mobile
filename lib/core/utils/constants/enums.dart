// List of Enums

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum ResponseStatus {
  initial,
  loading,
  inProgress,
  loadMore,
  success,
  delete,
  update,
  logout,
  failure,
  error,
  login,
}

// enum PaymentMethods { }
